# engineering.md — 工程级变更留痕

> 记录 `python/` 模块之外的工程变更：质量闸、CI、编排脚本、文档体系、协作纪律。
> 追加式，新的在下。每条四段：范围 / 变更 / 验证 / 回滚。样例代码与配置不进本文件。
> 目录纪律见 `AGENTS.md` §6：`docs/changes/` 只放 `{module}.md` 条目文件。

---

## lint-type-gate

- 范围：`pyproject.toml`、`Makefile`、`.githooks/pre-commit`、`.github/workflows/lint.yml`、`python/**` 与 `deploy/server1/airflow/dags/**` 的注解补齐、`docs/rules/` 三份文档的检查点
- 变更：`docs/rules/` 早就写明了要跑 ruff 与 mypy，实现侧却一直没配。本次把这条静态检查补成机器闸。配置唯一源是 `pyproject.toml`，`Makefile` 包一层命令并提供 `uvx` 兜底，提交前钩子与 CI 跑同一条 `make lint`。ruff 选 14 个规则类，行宽 120，docstring 用 Google 风格；mypy 打开 `disallow_untyped_defs` 与 `disallow_any_generics`。四类与中文写作冲突的规则（`D415`、`RUF001`、`RUF002`、`RUF003`）和与 PySpark 写法冲突的 `N812` 逐条排除，理由与命中数写在配置注释里。验收清单与开发流程文档改成引用 `make lint`，不再单列步骤。
- 验证：`ruff check` 从 166 个问题清零，`mypy` 从 86 个问题清零，其中补了 113 条 docstring 与 18 处类型注解。等价性三层证据：格式化前后按字节码指纹比对 202 个代码对象逐项一致；16 份导出 CSV 重生成的 md5 与基线 16/16 相同；端到端重置重跑 16 个环节全绿。CI 双向验证：正常提交通过，运行号 35237994950；故意留下未用导入的临时分支被拦下，运行号 35238108100，临时分支已删除。
- 回滚：删除 `pyproject.toml`、`Makefile`、`.githooks/`、`.github/workflows/lint.yml`，并把 `docs/rules/` 三份文档的检查点改回人工核对。代码侧的 docstring 与类型注解可保留，不影响运行。

## delegate-review-fixes

- 范围：`deploy/server1/airflow/dags/fr2052a_daily_batch.py`、`deploy/server2/run-daily-pipeline.sh`、`docs/business/` 五份业务文档、`docs/rules/PROJECT-STRUCTURE.md`、`AGENTS.md`、`README.md`
- 变更：三个只读子代理分别审应用代码、dbt 与 SQL、文档，共提出 80 条发现。已修 29 条，其中两处会真实误导运行：日批 DAG 把「施加授权与迁移」排在「导出报表」之后，与跑批脚本和接口文档都相反；源文件到位检查查的是容器内路径，而任务实际在宿主执行，加上错误被吞、`wc -l` 恒返回 0，导致这道检查永远通过。修复后授权环节前移，源文件检查挪进跑批脚本新增的 `check-source` 环节，判据由调用方改为看退出码。另补上缺失的 `verify_scd2` 任务（日批任务数 17 到 18），并按代码实际内容重写文档里的宏表、CLI 契约表、Topic 数与端口。剩 45 条涉及口径与设计取舍，未改，逐条登记在 `docs/AUDIT-2026-09-17-delegate-review.md`。审查期间一度新增 `.githooks/commit-msg` 强制提交标记，该钩子已按 `commit-message-policy` 撤销。
- 验证：`airflow dags list-import-errors` 无错误，日批 DAG 任务链与脚本步骤名逐一对照。`check-source` 正负两向实跑：源文件齐全时退出码 0，缺文件时退出码 1。文档侧按代码逐项复核命令签名与表名，README 里跑不通的参数已换成实际存在的选项。
- 回滚：`git revert 74fdcc7`。该提交只动编排、文档与钩子，不涉及数据表结构，回退后须重跑一次日批确认任务链顺序。

## commit-message-policy

- 范围：`AGENTS.md` §5、`docs/rules/CODING-STANDARD.md` Git 规范、`.githooks/commit-msg`、`docs/business/KNOWN-ISSUE.md`
- 变更：撤销「agent 提交必须带 `[AI]` 前缀」这一条，`.githooks/commit-msg` 删除。理由由用户给出：agent 是代用户执行，它提交的即用户提交，提交信息不该体现执行者是谁。提交信息按 `type: subject` 写，说明改了什么、为什么。代码文件头的 `[AI-GENERATED]` 与 `reviewed_by` 保留，那部分记录的是代码来源与评审状态，与提交归属无关。
- 验证：撤销后本地提交不再被拦截，提交信息形如 `fix: ...`。钩子目录只剩 `pre-commit`，它仍跑 `make lint`。`grep -rn "\[AI\]"` 在全仓只命中本文件与历史审计报告、构建日志。
- 回滚：恢复 `.githooks/commit-msg` 与两处文档措辞即可。回滚成本低，无数据影响。

## changes-trail-bootstrap

- 范围：`docs/changes/engineering.md`、`AGENTS.md` §6、`docs/rules/PROJECT-STRUCTURE.md` 目录职责表、`docs/business/KNOWN-ISSUE.md` 偏离表
- 变更：`AGENTS.md` 要求功能与契约变更在 `docs/changes/{module}.md` 追加条目，该目录却一直是空的，E6 与 E7 的变更散落在 `docs/build-log.md`。本次按格式补上条目，本文件即首个条目文件，同时成为后续条目的格式样例。`AGENTS.md`、`PROJECT-STRUCTURE.md`、`KNOWN-ISSUE.md` 里「目录为空」「模板待补」的口径同步更正。条目 slug 不再要求与分支同名，本项目直接在主分支推进，没有 feature 分支。
- 验证：目录内只有 `engineering.md` 一个条目文件，无 README 与附件。四段结构逐条自检：范围写清文件、变更写清动机与做法、验证给可复现的证据、回滚给具体动作。
- 回滚：删除本文件并恢复三处文档措辞。回滚后变更史重新退回只存在于 `docs/build-log.md` 的状态。

## table-contracts

- 范围：新增 `docs/tables/`（51 份契约）、`docs/rules/PROJECT-STRUCTURE.md`、`AGENTS.md` §9、`README.md`、`docs/business/PROJECT.md`、`docs/business/DATA-DESIGN.md` §2.1
- 变更：数据类项目的上游规范要求「每表一份 `docs/tables/{table}.md`」，本项目此前没有这个目录。本次补齐 51 份：ref 9 张、bronze 7 张、silver 明细 8 张、silver 汇总 5 张、版本历史 7 张、gold 与 ADS 报表 3 张、PostgreSQL 控制与审计 12 张。每份 13 节：层级 / 主题 / 粒度 / 业务主键 / 去重方式 / 分区 / 字段清单 / 金额单位约定 / PII 与脱敏 / 生命周期 / 新鲜度 SLA 与 owner / 上下游依赖 / 质量规则清单。字段清单不是手抄的：ref 与 bronze 从 `sql/iceberg/*.sql` 的 DDL 抽取，PostgreSQL 表从 `sql/postgres/*.sql` 抽取，silver 与 gold 从 dbt 模型 SQL 的最终投影抽取，版本历史表从 `owd_scd2.py` 的结构加 6 个版本列。文档地图、结构规范与 README 的目录清单同步登记该目录。
- 验证：51 份文件与 51 张表一一对应，逐份核对三件事：13 节齐备且每节非空、字段名与抽取结果逐项一致、无占位词。DDL 侧另做了抽取数与 DDL 声明数的逐表比对，28 张全部一致（例：`ads_fr2052a_report` 的 55 列覆盖 Section A–K 全部行项目）。不入契约的两类在 `DATA-DESIGN.md` §2.1 写明：连通性自检的探针表、只有主题声明的三个 Kafka 主题。
- 回滚：删除 `docs/tables/` 目录，恢复四处文档的目录清单。契约是纯文档，回滚不影响任何运行链路。

## bronze-dedup-key-fix

- 范围：`python/consumers/kafka_to_iceberg.py`、`python/alerts/realtime_scanner.py`、`docs/tables/ods_*.md`（7 份）、`docs/tables/ads_fr2052a_realtime_alerts.md`
- 变更：bronze 层 MERGE 的匹配条件和批内去重窗口都缺 report_date，同一源记录在不同报告日会被当成同一条，后一个报告日覆盖前一个。改为三列 source_system + source_record_id + report_date。实时扫描器的事件 ID 和落库 report_date 列原先取命令行常量，改为取消息体里的 report_date，使同一笔敞口跨报告日算出不同 ID。命令行 --report-date 保留，用途收窄为日志与不一致计数提示。
- 验证：自验四条断言全 PASS（MERGE 含 report_date、去重窗口含三列、落库 report_date 不再取命令行常量、事件 ID 的哈希输入含消息里的报告日）；`make lint` 三子命令全绿。上线实测三项：① 把 500 行源数据改标为 2026-09-17 重放后，`bronze.ods_deposits` 两期各 500 行并存（旧实现会把 2026-09-16 的 500 行改写成 2026-09-17，表内仍只 500 行，且行数核对照样通过）② 以 `REPORT_DATE=2026-09-17` 刻意传错运行参数执行实时扫描，落库 47 条事件的 `report_date` 全为消息里的 2026-09-16，并打印 500 条消息与运行参数不一致的提示 ③ 只清消费位点、保留事件表重放时，作业以主键冲突报错退出，确认「重复出现」仍是显式失败而不是静默重复。
- 回滚：`git checkout -- python/consumers/kafka_to_iceberg.py python/alerts/realtime_scanner.py docs/tables/`。回退后 bronze 层恢复两列匹配，实时事件 ID 恢复取命令行日期。

## submission-key-and-audit-table

- 范围：`sql/postgres/10_control_tables.sql`、`python/exporters/generate_submission.py`、`sql/admin/reset_demo.sql`、`python/alerts/realtime_scanner.py`、`docs/tables/ads_fr2052a_submission.md`、新增 `docs/tables/ads_fr2052a_submission_audit.md`、`docs/business/DATA-DESIGN.md`、`docs/business/PROJECT.md`、`docs/business/INTERFACE-DESIGN.md`
- 变更：报送台账唯一键从 `(report_date, entity_code, file_format, file_hash)` 改为 `(report_id, file_format)`，哈希从键里拿掉进入 `SET`。台账语义改为「当前状态」：一个文件一行，重生成时更新哈希与回执。新增审计表 `ads.ads_fr2052a_submission_audit`，一行一次重生成，记录 `previous_hash` 与 `is_content_change`。写入侧每次生成先查旧哈希、写审计行、再 upsert 台账，三步同一事务。迁移块用 `pg_constraint` 判旧约束存在再 DROP，新约束不存在才 ADD，重复执行不报错；表为空时整段 no-op。顺带删除 `realtime_scanner.py` 的 `detect()` 已无人使用的 `report_date` 参数。表契约数量从 51 改为 52。
- 验证：自验断言全 PASS（唯一键含 report_id + file_format、唯一键不含 file_hash、审计表结构与 CHECK、迁移块存在、写入侧冲突目标与审计行、复位脚本清审计表、detect 签名与调用点同步）；`make lint` 三子命令全绿。上线实测（真实 PG 与报送链路，非本地）：① 迁移前人为插入一行「上一版」台账（同 report_id + 格式、不同哈希）→ 台账 16 行 → `verify-submission` 报红「ENT001-FR2052A-20260916-01 CSV 哈希或大小不一致」，复现了本条发现 ② 跑迁移后：约束变为 `UNIQUE (report_id, file_format)`，多出的行被回填进审计表（`entry_source = MIGRATION`）并从台账删除，台账回到 15 行 ③ 重新生成后再核对恢复全绿，审计表累计 15 条 `GENERATE` 记录，其中内容真变过的那一条 `is_content_change = true` ④ 迁移后同一业务键再插第二行被数据库直接拒绝（`duplicate key value violates unique constraint`）——缺陷从「事后核对才发现」变成「根本不允许」。迁移的一条边界：它按 `submission_id` 最大者判定「最新行」并保留，无法校验哪一行的哈希与磁盘文件一致，那是 `verify-submission` 的职责。
- 回滚：`git checkout -- sql/postgres/10_control_tables.sql python/exporters/generate_submission.py sql/admin/reset_demo.sql python/alerts/realtime_scanner.py docs/tables/ads_fr2052a_submission.md docs/tables/ads_fr2052a_submission_audit.md docs/business/DATA-DESIGN.md docs/business/PROJECT.md docs/business/INTERFACE-DESIGN.md`。回退后台账恢复旧键，审计表删除，detect 恢复读死参数。

## run-context-and-submission-gate

- 范围：新增 `python/governance/run_context.py`、`docs/tables/ads_pipeline_run_context.md`；改 `sql/postgres/10_control_tables.sql`、`python/validators/check_submission_gate.py`、`sql/admin/reset_demo.sql`、`deploy/server2/run-daily-pipeline.sh`、`deploy/server1/airflow/dags/fr2052a_daily_batch.py`、`docs/business/DOMAIN-LANGUAGE.md`、`docs/business/INTERFACE-DESIGN.md`、`docs/business/DATA-DESIGN.md`、`docs/business/PROJECT.md`
- 变更：新增运行上下文表 `ads.ads_pipeline_run_context`，一行一次跑批：批次号、报告日、处理日、生效日、运行类型、状态、起止时刻。**日期口径编码进 schema**——`CHECK (processing_date = report_date + 1 AND effective_date = processing_date)`，口径若要变必须改约束，是一次看得见的动作而不是口头约定。新增 `run_context.py`（`--open` / `--close` / `--show`，`--close` 找不到行即报错退出，不许静默）。跑批脚本首尾各加一个环节 `run-context-open` / `run-context-close`，日批 DAG 链首链尾各加一个任务。**放行闸补两条判据**：① 本报告日**最近一次**日批必须是 `SUCCEEDED`（`ORDER BY started_at DESC LIMIT 1`，只判「最近一次」而不是「存在过某次成功」，否则就是本条发现的翻版）② 本报告日在 `ads_liquidity_metrics` 必须有行（判定痕迹）。两条不满足都按「无法判定」退 3，不放行。复位脚本清上下文表；词典补「报送时刻」条目并写明「报告日 / 业务日期」是同一值的两种叫法。表契约 52 → 53 份。
- 验证：静态——21 条断言（委托方独立复跑）、`py_compile` + `bash -n` + `make lint` 全绿。上线实测四项：① 最近一次 `SUCCEEDED` → 闸退出码 0，放行 ② 造一个更晚且未收口的批次（`RUNNING`）→ 闸退出码 3，输出「最近一次日批状态为 RUNNING，不是 SUCCEEDED」——这正是「最近一次」而非「存在过」的实证 ③ 从未跑过的报告日 → 退出码 3「运行上下文表里没有行」④ 跑批成功但无判定痕迹的报告日 → 退出码 3「流动性指标表里没有行」。Airflow 侧 `dags list-import-errors` 无输出、日批 DAG 在册。
- 备注（运维坑，实测踩到）：跑批脚本与 DAG 住 `deploy/` 树，由 `deploy/sync-deploy.sh` 同步；`deploy/server2/sync-app.sh` 只管 `app/` 树。第一次上线验闸时只同步了 app 树，于是「闸是新的、编排是旧的」，环节列表里根本没有 `run-context-open`——与 `sync-deploy.sh` 头部记录的 E4.5 事故同型。
- 备注（交付完整性）：本批委托会话被上游 429 限流中断在自验之前（日志尾 `API call failed after 3 retries: HTTP 429`），产物已落地但没跑自验：docstring 缺 `r` 前缀、两份文件未走 `ruff format`。复核时补跑 `make lint-fix` 并手工补前缀后全绿；**交付自报不可信，未自验的批更要逐项复跑**。
- 回滚：`git checkout -- sql/postgres/10_control_tables.sql python/validators/check_submission_gate.py sql/admin/reset_demo.sql deploy/server2/run-daily-pipeline.sh deploy/server1/airflow/dags/fr2052a_daily_batch.py docs/`，删除新增的 `python/governance/run_context.py` 与 `docs/tables/ads_pipeline_run_context.md`，并在库里 `DROP TABLE ads.ads_pipeline_run_context;`。回退后放行闸回到「只读熔断闸行」的旧判据。

## b1-verification-gates

- 范围：`python/lakehouse/verify_ods_schema.py`、`python/lakehouse/verify_bronze.py`、`python/lakehouse/load_ref_tables.py`、`python/lakehouse/verify_gold.py`、`python/lakehouse/verify_silver.py`、`python/lakehouse/restate.py`、`python/governance/pipeline_health.py`、`python/consumers/kafka_to_iceberg.py`、`python/exporters/generate_submission.py`、`sql/postgres/10_control_tables.sql`、`sql/iceberg/02_create_ods_tables.sql`、`deploy/server1/airflow/dags/fr2052a_daily_batch.py`、`docs/tables/ads_fr2052a_report_history.md`、`docs/business/INTERFACE-DESIGN.md`、`docs/business/MODULE-DESIGN.md`
- 变更：判据要能失败 + 单点对齐，共 10 条。判据类（前 5 条）：三个核对脚本在核对对象为零时退出 1 而非 0（零命中不算通过）；报表行形状从 `entity_rows >= 2` 改为三集合比对（报表实体 vs bronze 存款实体 vs ref 层级表实体）；折算核对从 inner join 改为以 ODS 上游为准的 left join，缺汇率的行统计为 unjoined 而非从分母消失；明细回溯把 NULL 与 0 的兜底删掉，命中零行、明细合计 NULL、报表侧 NULL 各自显式 FAIL；巡检用分别计数替代 `min(submission_status)`，被拒文件写进 finding。单点类（后 5 条）：重述快照哈希两侧统一走 `canonical_hash`（规范化 JSON 取 md5，禁在 SQL 侧算）；7 条 Kafka 流的临时视图按表名隔离防串写；重述登记关旧版本用 `greatest(date, begin_date)` 防区间反向，库侧补 `CHECK (end_date IS NULL OR end_date >= begin_date)` 约束；CSV 导出新增 `to_cell` 函数使布尔列输出 true/false 而非被当金额格式化；bronze DDL 注释从 `current_timestamp()` 改为按报告日 +1 天 02:00 派生，DAG 注释从 DROP + CREATE 改为 truncate=true。
- 验证：静态——委托方独立复跑 49 条结构锚定断言全 PASS；纯函数实测 `canonical_hash`（键序归一化、内容变即变、同内容同哈希）与 `to_cell`（布尔/数值/None/文本四类）通过；`py_compile` + `make lint` 三子命令全绿；`git status` 只含允许清单内文件。上线实测（两台服务器，非本地）：① 零命中 —— 空 data-dir、无生产者配置、空 ref 目录三种情形都退 1（旧实现都退 0），正常路径退 0 且打印「ref 9 张 + bronze 7 张」② 折算覆盖率 A/B —— 同一份「JPY 汇率挂空」数据下，旧判据在缺汇率 82 条时仍 PASS，新判据 FAIL 退 1；正常跑「上游 N 行 = 逐行重算 N 条、缺汇率 0 条」③ 报表实体覆盖 —— 正常 PASS（4 实体 + 1 合并行），突变探针让报表少一个实体即 FAIL「报表缺 ['ENT005']」④ 明细回溯 —— 突变探针把过滤条件写错即 FAIL「过滤条件未命中明细表」（旧实现因 `or 0` 报 PASS）⑤ 重述哈希 —— 先用 SQL 复现两侧 md5 不等（`old_equal = f`），再实测同一内容连跑两次 capture（第二次短路、不新增版本）、改一行数据后 capture 按 CORRECTION 登记 v2 ⑥ 区间方向 —— 传入早于旧版本生效日的日期时告警并收口，库侧 CHECK 直接拒绝反向区间行 ⑦ 巡检 —— 台账造 1 行 REJECTED 后旧口径 `min()` 仍给 ACCEPTED、新口径给 `ACCEPTED/REJECTED` 并报 finding ⑧ CSV 布尔列与 XML 同字段取值一致（`true` / `consolidated="true"`）⑨ 视图隔离 —— 7 条流整批重放后逐表行数与 CSV 完全一致（1505 行）⑩ 复核用的临时产物（探针副本、空目录、临时配置）与数据改动（报表金额、台账状态、版本历史）全部清场，基线回到原值。
- 备注（交付完整性）：本批委托会话在第 10 分钟被上游 429 限流打断在自验之前（日志尾 `API call failed after 3 retries: HTTP 429`）：16 个文件的改动全部落地，但 `ruff format` 未跑、两份自验脚本落在仓库根（`_verify_b1.py`、`_verify_b1_func.py`）。复核时补跑格式化、删掉越界的临时文件、独立复跑全部断言后全绿。**未自验的批要按「产物已落地、证据未生成」对待。**
- 备注（一条限定）：第 10 条的跨表串写是并发形态，单机复现需要两个微批重叠，本次只做到「结构级修复 + 整批跑无跨表行」；`tmp_probe_*` 之类探针只在服务器临时生成、跑完即删，不入仓库。
- 回滚：`git checkout --` 恢复上述 16 个文件。库侧约束 `ads_fr2052a_report_history_interval_ck` 如已添加则 `ALTER TABLE ads.ads_fr2052a_report_history DROP CONSTRAINT ads_fr2052a_report_history_interval_ck;`。回退后核对脚本恢复零命中即通过、哈希两侧分算、视图同名共享。

## b2-silent-failures

- 范围：`python/validators/run_dq_rules.py`、`python/exporters/export_gold_to_pg.py`、`python/exporters/generate_submission.py`、`python/audit/time_travel.py`、`docs/business/INTERFACE-DESIGN.md`、`docs/business/MODULE-DESIGN.md`
- 变更：四处静默失败与注入面修复。① `run_dq_rules.py` 的 `evaluate_rule` 已返回四元组（违规数、明细、已评估表数、被跳过的表清单），但 `main` 的调用点仍按三元组解包（会 ValueError），补完为四元组解包并统一用 `detail_text`：全部跳过时写清跳过了哪些表，部分跳过时写出覆盖数与跳过清单，ERROR 级规则若有表被跳过即判 FAIL（覆盖面缩水不能算通过）。② `export_gold_to_pg.py` 的 `target_columns` 原先用宽 `except` 把一切异常都当 None（表不存在）。复核时查出比原判更重的根因：该函数把 `properties` **按位置参数**传给了 `spark.read.jdbc`（第三位是 `column`），Spark 断言 `lowerBound can not be None when column is specified` 抛 AssertionError，正好被那个宽 `except` 吞成 None —— 结构比对从来没有跑过。修法：删掉宽 `except`，改先判表是否存在再读列（判存在用 `pg_catalog` 而不是 `information_schema`：后者只列出当前角色有权限的对象，「表在但读不到」会被它藏成「不存在」），表存在但读不到列时让异常向上传播到外层逐表 `except`（行为可见：打印错误、记入失败清单、终局退 1），两处调用改成 `properties=properties` 关键字传参。③ `generate_submission.py` 在打印回执明细后加检查：任一回执 `accepted` 不为真即打印提示并退 1，台账写入行为不变（REJECTED 也登记）。④ `time_travel.py` 加白名单正则 `TABLE_RE`（库.表格式）与 `IDENTIFIER_RE`（标识符格式），加 `sql_literal` 函数（单引号翻倍转义），`main` 在进 Spark 之前逐项校验四个参数，非法打印原因并退 2，`--diff` 快照号转 int 后再进 SQL，`--trace-key` 走 `sql_literal` 转义后进 SQL。
- 验证：静态——委托方独立复跑 26 条结构锚定断言全 PASS（含「SQL 侧只出现转义后的写法」这类反向断言）；纯函数实测 `sql_literal` 三例与两个白名单正则的正反例（合法表名通过、带分号/两层点的表名被拒）通过；`py_compile` + `make lint` 三子命令全绿。上线实测（两台服务器，非本地）：① DQ 覆盖面 A/B —— 给 VDQ-002 混入一张没有该规则列的 ref 表后，旧判据仍 PASS 退 0，新判据 FAIL「已评估 7/8 张，跳过 ['ref.ref_entity_hierarchy']」退 1；正常跑 20 条规则覆盖数全部满分（7/7、5/5、1/1），无一条缩水，批次结论与修复前一致 ② 回执被拒 —— 外部回执文件造 5 个主体全被拒时退 1 且台账 15 行全登记 REJECTED，换回模拟回执再跑退 0、台账回到 15 行 ACCEPTED ③ 导出结构比对 —— 旧实现（复现版）对**已存在**的表返回 None（比对永远跳过）；新实现 表存在可读 → 55 列、表不存在 → None、表存在但无权限读 → 抛异常；导出环节整跑退 0，三张表回读一致 ④ 注入面 —— 非法 `--table`／`--key-column`／`--diff` 三种都退 2 并打印非法值原文；键值 `DEP-000001'; DROP TABLE silver.owd_deposits; --` 被当成普通字面量（无命中、退 0），冰表快照数不变。复核期的临时产物（探针副本、探针 schema 与角色、临时配置）与数据改动（台账状态、质量日志探针批次）全部清场。
- 备注（复核查出的根因比原判更重）：第 12 条原判是「宽 `except` 吞掉异常」，实测复现时定位到真正的触发点是 `spark.read.jdbc(url, query, properties)` 的位置参数误用 —— 第三个位置参数是 `column` 而不是 `properties`，Spark 在进入 JDBC 之前就断言失败，于是**每一次**调用都落进宽 `except`。含义是：那层「模型列 vs 目标表列」的防漂移比对自建立起从未执行过。只看代码形态（`try/except` 吞异常）会低估它，必须实跑才能看见。
- 备注（探针的一次失败也有价值）：第一次把探针脚本放在 `python/lakehouse/` 下用 `../validators/` 相对路径调 `spark-submit` 包装脚本，被拒（退出码 2）；探针脚本要与被探测的脚本同目录、用容器内绝对路径调用。
- 备注（判存在用什么口径）：`information_schema.tables` 只列出当前角色有权限的对象，用无权限角色实测该视图 0 行、`pg_class` 1 行；因此判存在必须走 `pg_catalog`，否则「表在但读不到」会被当成「表不存在」，又退回静默跳过。
- 回滚：`git checkout -- python/validators/run_dq_rules.py python/exporters/export_gold_to_pg.py python/exporters/generate_submission.py python/audit/time_travel.py docs/business/INTERFACE-DESIGN.md docs/business/MODULE-DESIGN.md`。回退后 `evaluate_rule` 的四元组返回值仍在，但调用点恢复三元组解包（会 ValueError，需同时改回 `evaluate_rule` 签名）；`target_columns` 恢复宽 `except`；报送脚本恢复退 0；审计脚本恢复无校验直接进 Spark。

## c1-fx-consistency

- 范围：`python/generators/ref_data.py`、`python/generators/generate_sample_data.py`、`dbt/models/staging/stg_fx_rates.sql`、新增 `dbt/tests/assert_fx_covered.sql`、`deploy/server2/run-daily-pipeline.sh`、`docs/business/{DATA-DESIGN,MODULE-DESIGN,INTERFACE-DESIGN}.md`
- 变更：把「汇率与测试数据一致」从**构造关系**变成**可失败的断言**。① 汇率契约写进 `_generate_exchange_rates` 的 docstring：汇率表是正常状态下的全量币种来源，ODS 抽到的币种必须都在汇率表里；唯一例外是故意注入。② 新增 `--inject-missing-fx`（逗号分隔）：故意不写这些币种的汇率行，但 **ODS 的抽币种池不收缩** —— 否则数据里根本不会出现该币种，缺汇率的场景反而造不出来（这是实现时踩到并改掉的坑）。③ 生成器自检新增「汇率覆盖」项，比对基准是**写出的汇率 CSV**（不是内存池），按 `(报告日, 币种)` 精确比对，注入币种放行并明示。④ `ref_exchange_rates` 的期望行数由 `len(FX_RATES) + 1` 派生，不再硬编码 10。⑤ `stg_fx_rates` 限定 `to_currency = 'USD'`（多目标币种会行放大）。⑥ 新增 singular test `dbt/tests/assert_fx_covered.sql`：ODS 六张明细表出现的 `(report_date, currency)` 组合在汇率表里缺 MID 汇率即失败。⑦ `dbt-run` 环节在 `dbt run` 之后追加 `dbt test`，断言不过即环节失败、日批红。
- 验证：静态——生成器本机跑通（正常 10 行汇率、全部自检通过；注入 JPY 时汇率 9 行、数据里仍有 249 行 JPY、自检打印 `[INJECT]` 放行）；`make lint` 三子命令全绿。上线实测（Server 2，Iceberg + dbt 真跑）：① 正常数据 → `ref-load + dbt-run` 退 0，`dbt test` 单独选中 `assert_fx_covered` 为 PASS=1 ② 注入 JPY 重建数据 → 探针确认「汇率表 9 个币种（无 JPY）、bronze 存款有 82 行 JPY」的缺陷状态真实存在 ③ 同一状态下跑日批 `dbt-run` → 断言 `ERROR=1`（「Got 1 result, configured to fail if != 0」）、环节 `[FAIL] 退出码 1`、整条日批 RC=1 ④ 恢复正常数据后 `ref-load + dbt-run` 退 0。
- 备注（同步边界，第二次踩）：第一次负向实测「环节没报红」，原因是只跑了 `deploy/server2/sync-app.sh`（app 树），没跑 `deploy/sync-deploy.sh`（deploy 树）——服务器上的跑批脚本还是旧的，`dbt test` 那一行根本不存在。这与 A3 批次踩的是同一个坑，已在技能里记为铁律：**验收前两个同步边界都要走，或先跑 `sync-deploy.sh --check`**。
- 备注（教训）：`dbt run` 与 `dbt test` 的汇总行都形如 `Done. PASS=N`，只看汇总行会把 `run` 的 PASS=16 误读成「16 条断言都过了」。判断断言有没有跑，要按测试名 grep 或先 `dbt ls --resource-type test` 数一遍。
- 回滚：`git checkout -- python/generators/ref_data.py python/generators/generate_sample_data.py dbt/models/staging/stg_fx_rates.sql deploy/server2/run-daily-pipeline.sh docs/`，删除 `dbt/tests/assert_fx_covered.sql`。回退后汇率覆盖只剩构造关系、没有断言，日批也不再跑 `dbt test`。

## c2-report-date

- 范围：`dbt/models/marts/ads_fr2052a_report.sql`、`dbt/models/marts/ads_gl_reconciliation.sql`、`python/generators/{config,ref_data,ods_data,generate_sample_data}.py`、`python/lakehouse/verify_gold.py`、`docs/business/{DATA-DESIGN,MODULE-DESIGN,INTERFACE-DESIGN}.md`
- 变更：把 `report_date` 从「一列标签」变成**贯穿所有聚合的分组维度**。① 报表模型的 8 个口径 CTE（存款/有担保融资/现金/贷款流入/HQLA/衍生品/或有负债/30 天现金流）各自只按 `entity_code` 分组，多期共存时不同报告期的金额会加到一起；现在按 `(report_date, entity_code)` 分组，8 个 `left join` 也补上 `report_date` 匹配，合并行由 `max(report_date)` 改为按 `report_date` 分组。② 对账模型同样处理，并删掉 `select max(report_date) ... cross join` 的单行日期构造：`gl_by_section` 与 `report_amounts` 各自按期汇总，两边按 `(section_code, report_date)` 对上。③ 样本生成器加 `--report-days N`（默认 1），多期数据按期生成；`ods_rng(表名, 报告日)` 取代 `table_rng(表名)`，让**同一个报告日的数据与本次生成了几期无关** —— 否则加期会把已有期的随机序列推后，多期回归就无从比对。④ 汇率改由 `ref_data._generate_exchange_rates` 按报告日逐期出行（每期 10 个币种），生成器不再对 CSV 做事后追加。⑤ 自检适配多期：报告日集合、行数按期累加、日期先后以**每行自己的**报告日为准、总账**逐期**判平衡。⑥ `write_csv` 支持追加写，多期第一期覆盖建表、后续期追加数据行，表头只出现一次。
- 验证：静态 —— `make lint` 三子命令全绿；结构锚定：`group by report_date, entity_code` 8 处、带 `report_date` 的 `left join` 8 处、裸 `group by entity_code` 0 处、`max(report_date)` 0 处（全 marts）、`cross join` 0 处。生成器等价性（关键自证）——`--report-days 1` 与 `--report-days 2` 两个目录比对：7 张 ODS 表与汇率表在 `2026-09-16` 这一期的行**逐字节一致**，2 期行数 = 1 期的 2 倍，非汇率 ref 表逐字节一致，两种模式全部自检 PASS（含 2 期各自的总账平衡）。上线实测（Server 2，Iceberg + dbt 真跑整条日批 18/18 全绿）：改造前先快照 PG `ads` 层 13 张表的行数与内容 md5，改造后同批数据重跑 —— 报表、明细、对账、报送台账、版本历史、重述记录**逐字节相同**（5 行/569 行/8 行 等）；另 5 张表因携带运行时刻戳而 md5 变化，其非时刻戳字段逐一核对与基线一致（如流动性指标 `lcr_ratio` 5.8944/14.8091/4.5054/4.3555/4.3783，与基线记录的四舍五入值一致）。
- 备注（多期 A/B，旧模型下缺陷长什么样）：把改造前的两个 marts 模型单独推回 Server 2、在同一份 2 期数据上重跑，缺陷完整复现 —— 两期实体行各自都是**两期之和**（ENT002 在 2026-09-15 与 2026-09-16 都是 584,214,848.40），2026-09-15 一期**没有合并行**（合并行被 `max(report_date)` 兜到 09-16 那一行，金额是两期实体之和 4,388,672,116.78）。这同时说明本批的多期样本真的能打到这个缺陷上，测试不是空跑。恢复后锚定期切片回到改造后的正确值。
- 备注（核对脚本也要逐期）：`verify_gold.py` 的实体覆盖、合并加总、明细回溯、二级资产上限、流入上限五项原先都假定「全表只有一期」——多期数据下 `collect()[0]` 会取到任意一期，`consolidated_rows == 1` 直接失败。本批一并改成逐报告期核对（结论名带报告期），并新增 `report_periods()` 作为期间清单来源。
- 备注（同日为何要动生成器的随机源）：把报告日编进种子是本批的**关键取舍**。代价是单期样本数据不再是改造前那批（种子变了），收益是「加期不扰动已有期」这条性质成立，而它正是多期回归唯一可用的判据 —— 没有它，1 期与 2 期的同一报告日无法逐字节比对。样本数据本身是构建产物，随后走基线刷新。
- 回滚：`git revert` 本次提交即可；模型侧回退后多期数据会重新出现跨期串加，数据侧回退后 `--report-days` 参数与多期自检消失、汇率表回到只有报告日一天。

## c3-1-intracompany-parent-gl

- 范围：`python/generators/{config,ref_data,ods_data,generate_sample_data}.py`、`sql/iceberg/01_create_ref_tables.sql`（`ref_counterparty.counterparty_type` 列注释）、`docs/business/DATA-DESIGN.md`、`docs/changes/engineering.md`
- 变更：样本数据从「只有子公司单体」一档扩展到三个报送视角都有数据支撑。① `ref_counterparty` 追加 5 行集团内对手方（CP9001–CP9005，`counterparty_type = 'AFFILIATE'`，LEI 取自 `ref_entity_hierarchy` 同一实体的值），表行数 50 → 55，期望行数改为派生（外部 50 + 集团内 5）。② 母公司 ENT001 开始记账：对第三方对手方的存款/回购/贷款/证券/衍生品/表外承诺追加行（小账，金额区间上限 × 0.08），既有四家子公司的生成循环一行不改。③ 集团内往来配对腿：每对（母公司 ↔ 一家子公司）生成存款腿（母公司负债，`customer_type_raw='AFFIL'`）和贷款腿（子公司资产，`counterparty_id=CP9001`），两条腿金额来自同一常量 `INTRACOMPANY_PAIRS`，天然相等。④ 总账从「一本账挂在 ENT001」改为逐实体一本账：每个 BOOKING_ENTITIES 各生成一组科目余额，由该实体自己的明细倒推，各自借贷平衡；`gl_break_amount` 落点从全局 2100 收窄到 ENT002 的 2100；总账行数由 `GL_ROWS_PER_ACCOUNT × len(GL_ACCOUNTS) × len(BOOKING_ENTITIES)` 派生。⑤ 行数自检全派生（单期 = 子公司配额 + 母公司追加 + 配对腿），新增内部往来配对自检（逐对核对存款腿本金 == 贷款腿 outstanding 且两侧实体与对手方编号互对），总账平衡自检改为逐实体 × 逐报告期。⑥ 随机抽取池**只放外部对手方**（`ref.counterparties` 滤掉 `AFFILIATE`，`ref.affiliate_counterparties` 单独存）：集团内对手方只在刻意构造的配对腿里出现。这条是复核时改的 —— 实现初稿把集团内对手方混进了随机池，数据里出现了 21 条只有一条腿的内部头寸（例如对母公司的回购却没有对应另一腿），合并抵销会只抵一边、把差额留在合并口径里。
- 验证：静态 —— `make lint` 三子命令全绿。生成器自检 —— 单期全部 PASS，其中总账平衡按**实体 × 报告期**逐本判（ENT001 88,718,857.53 对子公司的 17 亿–39 亿量级，母公司确是小账），内部往来配对自检「全部匹配」。等价性（本批最关键的证据）—— 与改造前（`git worktree` 取 HEAD）逐行比对：6 张业务表里**四家子公司的既有行逐字节相同**（贷款表新增的 4 条配对腿是追加行，既有行前缀相同）；内部往来只出现 8 行 = 4 对（存款腿全在 ENT001、金额 3,000,000/1,200,000/2,500,000/800,000，贷款腿在四家子公司、金额一一相等），回购/证券/衍生品/表外承诺表集团内行数为 0。多期性质 —— `--report-days 1` 与 `2` 的锚定期行逐字节一致，行数为 2 倍（存款 544→1088、贷款 329→658、总账 275→550、汇率 10→20）。
- 回滚：`git checkout -- python/generators/{config,ref_data,ods_data,generate_sample_data}.py sql/iceberg/01_create_ref_tables.sql docs/business/DATA-DESIGN.md docs/changes/engineering.md`。回退后母公司不记账、无集团内对手方、总账回到单本挂 ENT001。

## c3-2a-three-poses

- 范围：`dbt/models/staging/owd_{deposits,loans,secured_financing,securities,derivatives,off_bs}.sql`、`dbt/models/intermediate/ows_cashflow_projection.sql`、`dbt/models/marts/{ads_fr2052a_report,ads_fr2052a_detail,ads_gl_reconciliation}.sql`、`dbt/macros/fr2052a_rules.sql`、`python/lakehouse/{verify_gold,verify_scd2,owd_scd2}.py`、`sql/postgres/10_control_tables.sql`、新增 `sql/iceberg/08_owd_history_add_intracompany.sql`、`deploy/server2/run-daily-pipeline.sh`、`docs/business/{DATA-DESIGN,MODULE-DESIGN,INTERFACE-DESIGN}.md`、`docs/tables/` 三份
- 变更：把报送口径从「实体行 + 一行合并行」扩成**三档视角**，并让合并口径真正抵销集团内往来。① OWD 六张明细加 `is_intracompany`，判据收在一个宏 `is_affiliate_counterparty` 里（`counterparty_type = 'AFFILIATE'`）；`customer_segment` 宏补 `AFFIL → AFFILIATE`。② 报表模型把 `is_intracompany` 当**分组维度**（不是复制两份列）：8 个口径 CTE 按 `(report_date, entity_code, is_intracompany)` 分组，其上再汇总出法人实体单体行（两种标记都含）与合并行（只含 `false`）；HQLA 40% 上限与流入 75% 上限在合并口径按抵销后的金额重算。③ 合并行的 `entity_code` 改用保留码 `GRP001`；口径码三档 `01/02/03` 由 `is_consolidated` + `entity_code` 推出，不另加来源不明常量列。④ 现金流量预测把 `is_intracompany` 一并透传，否则会出现「Section C 抵销了、Section K 没抵销」的单边不一致。⑤ 明细表加同名列（PG 侧走显式迁移，判存在用 `pg_catalog`）。⑥ 对账模型重构为「报告期 × 视角」：法人单体行对各自总账（两侧都不抵销）、合并行对 `Σ 实体总账 − 集团内往来`（两侧一致剔除，存款腿对 C、30 天内到期的贷款腿对 F）。⑦ 版本历史表补列走一次性迁移 `sql/iceberg/08_*`，`owd_scd2.py` 加列集合前置检查，缺列时直接指出该跑哪个迁移。
- 验证：静态 —— `make lint` 三子命令全绿。迁移 —— 6 张 `silver.owd_*_history` 补列成功（`ADD COLUMN IF NOT EXISTS` 在 Spark/Iceberg 下不支持，改成一次性 ALTER 并写明只跑一次）。端到端 —— 两期样本跑整条日批 **18/18 全绿**；`verify-ads` 24 项全过：每期 6 行（GRP001(01) + ENT001(03) + 四家子公司(02)）、每期恰 1 行合并行、实体集合与 ref 一致、合并口径 8 列逐列「Σ实体 − 抵销 = 合并行」成立（抵销项存款腿 7,500,000.00）、明细回溯逐 Section 逐期吻合、二级资产上限与流入上限逐期验算、GL 对账每期 48 行（6 视角 × 8 Section）全部 PASS。独立复算 —— 抵销金额另从 `sample_data/ods/*.csv` 直接算出（存款腿 7,500,000 / 贷款腿 7,500,000，两条腿金额相等），与核对脚本读数一致；PG 侧切片复核：`Σ 实体 C − 7,500,000 = 合并行 C` 两期都成立。母公司单体行不再是空行：ENT001 的 Section C 16,265,231.77、Section E 1,951,827.81（来自它自己那本总账）。
- 备注（用旧样本跑新模型会误导判断）：第一次上线核对时父公司 Section C 是空的，看起来像模型缺陷，实际是 `sample_data/` 还是 C3-1 之前生成的（那批数据里母公司不记账）。换数据重跑即正常。教训：**改完生成器必须重新生成样本再验模型**，两个版本的数据混用会造出不存在的缺陷。
- 备注（三条上线核对期发现的新缺陷已登记在 `docs/AUDIT-2026-09-17-delegate-review.md`）：`dbt-run` 环节因 bash 条件上下文导致 `set -e` 失效、只取最后一条命令的退出码（dbt run 失败被 dbt test 掩盖）；`verify_scd2` 用有序列表比业务列，列位置一变就误报且差异清单为空；`verify_gold` 的抵销核对把全部集团内放款都从 Section F 减掉，而 Section F 只含 30 天内到期的部分。
- 回滚：`git revert` 本次提交，并回退历史表列（`ALTER TABLE silver.owd_*_history DROP COLUMN is_intracompany`，6 张）、PG 两列（`ads.ads_fr2052a_detail.is_intracompany`、`ads.ads_gl_reconciliation.entity_code`）。回退后合并行回到 `ENT001` 两档口径、集团内往来不再抵销。

## c4a-caliber-batch

- 范围：`dbt/dbt_project.yml`、`dbt/macros/fr2052a_rules.sql`、`dbt/models/staging/owd_deposits.sql`、`dbt/models/intermediate/ows_cashflow_projection.sql`、`dbt/models/marts/{ads_fr2052a_report,schema.yml}`、新增 `dbt/tests/{assert_behavior_covered,assert_insured_within_limit}.sql`、`python/generators/{ref_data,generate_sample_data}.py`、`python/lakehouse/{verify_silver,verify_gold}.py`、`python/alerts/liquidity_monitor.py`、`sql/postgres/10_control_tables.sql`、`docs/business/DATA-DESIGN.md`、`docs/tables/{ads_fr2052a_report,owd_deposits}.md`
- 变更：五组口径修正（编号对应 `docs/AUDIT-2026-09-17-delegate-review.md` 第二节）。① **活期/储蓄进 30 天流出**（#1）：新增 `behavioral_bucket` 宏，无到期日的活期/储蓄按行为口径归 `O/N`（原来落 `OPEN`，被 30 天窗口过滤 —— 全部活期存款不进流出）。② **行为假设对齐且不许静默兜底**（#9）：join 补 `customer_segment`；`ref_behavior_assumptions` 从 6 行手工样本改为按规则派生的全覆盖矩阵（80 行 = 4 产品类别 × 5 客户分段 × 到期桶）；删掉 `coalesce(runoff_rate, 0.1)`；覆盖由两道闸守（生成器自检 + `assert_behavior_covered`）。③ **受保金额按客户聚合、先折算再截断**（#11/#12）：`least(principal × spot_rate, 限额)`，限额按「客户 × 法人实体」聚合后按占比分摊；报表新增 `sec_c_insured_total` 让该口径有消费者；限额单源进 `dbt_project.yml`，断言 `assert_insured_within_limit`。④ **Section E 列名与语义一一对应**（#13）：库存现金、同业存放各自成列，央行存款保持 NULL（本演示无该科目），删掉与合计同值的 `sec_e_cash_equiv_total`；PG 侧显式迁移加两列删一列。⑤ **LCR 分子纳入现金与同业存放**（#15）：`hqla_l1_unencumbered_usd` = 非受限一级证券 + `sec_e_cash_total`，L2 的 40% 上限基数同步纳入
- 验证：静态 —— `make lint` 三子命令全绿。生成器自检 —— 新增两项全过（行为假设覆盖：ODS 探查组合 3656、假设表 80 行全部命中；定期存款到期日非空）。闸的 A/B —— 删掉假设表里 `(DEMAND, RETAIL, O/N)` 一行，生成器自检 FAIL 并指出缺哪个组合；受保方面样本里有 3 个客户受保总额超限且持多笔，逐笔口径会比按客户口径多算 532,555.04，因此按客户聚合不是等价改写。端到端 —— 两期样本整条日批 **18/18 全绿**（EXIT=0）。独立复算 —— 另写脚本只读 `sample_data/ods/*.csv` 与 `ref/*.csv`，按流失率/汇率/受保限额手算一遍，与 PG `ads` 层逐项比对 **74 项全中**（差 ≤ 0.05）：`sec_c_total`、`sec_c_insured_total`、`sec_e_cash_total/on_hand/due_from_banks`、`sec_k_total_outflows`、合并口径三条恒等式、`ads_liquidity_metrics` 的 L1 恒等式。PG 列结构 —— `sec_e%` 列恰为四项、`sec_e_cash_equiv_total` 已消失
- 备注（口径修正对 LCR 的净影响，两处修正方向相反）：现金进分子抬高 LCR、活期流出进分母压低 LCR，分母效应更大 —— 合并口径 LCR 4.8907 → 4.6952（分子 L1 +138,163,188.84、分母 +58,054,209.10）；五家实体同向下降（如 ENT004 6.9648 → 6.4065）
- 备注（连带暴露的第四项缺陷）：抵销核对原来只抵余额列，不抵现金流列。活期归 `O/N` 之后集团内存款腿开始产生流失额，实体行含、合并行不含，`sec_k_total_outflows` 逐期差 135,000（= 7,500,000 × 1.8%）。已修：抵销项补「存款腿 30 天流失额」，按 `ref_behavior_assumptions` 从 silver 明细独立算出。教训：**一个判据的漏算修好之后，相邻判据的漏算会立刻显形**，改口径要连带扫一遍同族的核对项
- 备注（列名取舍）：库存现金这一列最终叫 `sec_e_cash_on_hand` 而不是任务书初稿的 `sec_e_cash_and_equivalents` —— 列名要如实描述它装的是什么，GL 1001 是库存现金，不是「现金及等价物」；Section E 的合计列 `sec_e_cash_total` 才是这一族的合计
- 回滚：`git revert` 本次提交；PG 侧需手工回退列（删除 `sec_c_insured_total`、`sec_e_cash_on_hand`、`sec_e_due_from_banks`，恢复 `sec_e_cash_equiv_total`）；样本数据需用旧版生成器重新生成（假设矩阵变化会改变 Section K 与 LCR 的数值）

## c5-governance-batch

- 范围：`sql/postgres/10_control_tables.sql`、`sql/admin/reset_demo.sql`、新增 `sql/iceberg/09_owd_derivatives_history_add_mtm_currency.sql`、`sql/iceberg/oneoff/`（05/06/07 三个一次性脚本移入，内容未改）、`dbt/models/{staging,marts}/schema.yml`、新增 `dbt/tests/{assert_one_consolidated_row_per_period,assert_pose_code_consistent}.sql`、`dbt/models/staging/{owd_securities,owd_derivatives}.sql`、`dbt/models/marts/{ads_fr2052a_report,ads_fr2052a_detail}.sql`、`python/generators/{ref_data,ods_data,generate_sample_data}.py`、`python/validators/run_dq_rules.py`、`python/lakehouse/verify_gold.py`、`deploy/reset-demo.sh`、`config/liquidity_thresholds.json`、`docs/rules/{PROJECT-STRUCTURE,ACCEPTANCE-CHECKLIST}.md`、`docs/business/{DATA-DESIGN,KNOWN-ISSUE}.md` 与相关表契约
- 变更：本批没有新功能，两件事 —— **把已经声明但没生效的东西变成真生效**，外加四个小口径。① 三张报表表补唯一约束（`report_id` / 明细九维 / 对账 `报告日×实体×Section`）；② dbt 层补 18 条 schema 断言 + 2 条 singular test（每期恰一行合并行、口径码与 `is_consolidated`+`entity_code` 自洽）；③ 二级资产上限核对改为从 silver 明细独立复算输入，消掉自证循环；④ 质押标记统一成布尔（只有明确 `Y` 算受限，NULL 按未受限），源系统缺报由新增 `VDQ-021`（WARNING）报出来；⑤ 衍生品盯市值按 ODS 声明的 `mtm_currency` 折算，并把币种与汇率输出成列，总账侧 1500/2200 同步改（只改一侧会让差异伪装成「对账不平」）；⑥ Section F 的 30 天窗口加下界；⑦ 明细的逆回购方向归位（不再让下游靠 `line_item` 过滤）；⑧ 危险脚本移入 `oneoff/`，规则文档写明常规路径不得引用；⑨ 复位脚本不再清版本历史表；⑩ 文档漂移逐条改正，三张无消费者的 OWS 表登记为待下线
- 验证：静态 —— `make lint` 全绿（40 个文件）。端到端 —— 两期样本整条日批 **18/18 全绿**，耗时 **3 分 55 秒**。新增闸的**负向实测**（全部实做）：三条唯一约束各插一行重复数据，全部被拒且行数保持 12 / 1220 / 96 未污染；两条 singular test 用探针实造缺陷（把某期合并行的 `is_consolidated` 改成 false、把一行 `report_id` 的口径码改错）→ 断言 FAIL、改回后 `dbt test` **23/23** 全绿；`VDQ-021` 实报 17 行违规且 WARNING 不阻断环节。独立复算 —— 沿用 C4a 的 CSV 复算脚本，74 项仍与 PG `ads` 层逐项一致。PG 恒等式 —— 「非受限四项 + 已受限 = Section G 合计」10/10 实体行成立（质押标记 17 行缺报的情况下，这正是布尔统一的直接效果）
- 备注（探针揪出新判据自身的缺陷，本批最有价值的一次发现）：新写的合并行断言写成 `where is_consolidated group by report_date having count(*) <> 1`，只能抓「多于一行」；**合并行整个缺失**时过滤后一行不剩、`group by` 不产生分组、`having` 从不求值，测试照样绿 —— 最严重的形态恰好漏网。修法：基准取「报表里出现过的全部报告期」再左连计数补 0。教训：**新加的闸必须反向实测**，否则「判据在、但漏掉最严重形态」会一路绿着走
- 备注（基线刷新）：LCR 基线区间 4.4~14.8 → **4.07~11.96**（`config/liquidity_thresholds.json`）；验收清单同步刷新环节数 16→18、校验规则 20→21、血缘 48/20→49/28、全链路耗时 3 分 11 秒→3 分 55 秒
- 回滚：`git revert` 本次提交；PG 侧删三条唯一约束；若已执行过 `09_*` 迁移，历史表那两列需手工 `DROP COLUMN`

## c6-recon-independent-benchmark

- 范围：`config/pipeline_topics.json`、`sql/iceberg/02_create_ods_tables.sql`、`sql/iceberg/oneoff/06_rebuild_owd_history.sql`、`sql/postgres/10_control_tables.sql`、`python/generators/{config,ods_data,generate_sample_data}.py`、新增 `dbt/models/staging/owd_treasury_cash_position.sql`、`dbt/models/{sources.yml,staging/schema.yml,marts/schema.yml}`、`dbt/models/marts/ads_gl_reconciliation.sql`、新增 `dbt/tests/assert_recon_benchmark_independent.sql`、`python/lakehouse/{verify_gold,verify_silver,owd_scd2,verify_scd2}.py`、`python/validators/run_dq_rules.py`、`deploy/server2/run-daily-pipeline.sh`、`deploy/server1/airflow/dags/fr2052a_gl_reconciliation.py`、`docs/business/{DATA-DESIGN,INTERFACE-DESIGN,DOMAIN-LANGUAGE,KNOWN-ISSUE}.md`、`docs/tables/ads_gl_reconciliation.md`、新增 `docs/tables/ods_treasury_cash_position.md` 等三份表契约、`docs/rules/ACCEPTANCE-CHECKLIST.md`
- 变更（对应审查处置表第 2 条「GL 对账 Section E 自比对」）：给 Section E 换上**独立基准**，让这条对账能真正查到错。① 新增 ODS 表 `bronze.ods_treasury_cash_position`（主题 `treasury_cash_position`，来源系统 `TREASURY_SYS`）：库存现金盘点数 + 各代理行对账单余额 + 未达账项明细，每期 20 行（5 实体 × (1 库房 + 3 代理行)）。② 对账模型的基准侧拆成两类：非现金 Section 取总账科目余额（`benchmark_source = GL`），Section E 改取司库现金头寸（`TREASURY_CASH_POSITION`）；`gl_amount` 列随之改名 `benchmark_amount`（名字不改就是误导），新增 `benchmark_source` 与 `reconciling_item_usd` 两列。③ 判定公式统一为 `调节后差异 = 基准金额 + 调节项 − 报送金额`；容差分两档 —— 总账口径沿用 1% 且不少于 1 分钱，对账单口径只留 1 分钱（差额已被调节项逐项解释，再留 1% 等于放行几十万的错误）。④ 新增断言 `assert_recon_benchmark_independent`：Section E 的基准来源必须是司库现金头寸、基准与报送金额必须不等、调节项必须非零 —— 三条判据分别抓「基准改回总账」「两侧恰好同值」「未达账项被抹平」。⑤ 生成器新增 `generate_treasury_cash_position`（在总账之后运行，对账单余额以该期账面为锚），并新增自检「现金头寸勾稽」与「独立基准非零」两项。⑥ 新表按既有层契约铺开：staging 模型走 `stg_fx_rates` 折美元、`owd_scd2`/`verify_scd2`/`verify_silver`/`run_dq_rules`（VDQ-002、VDQ-005）四份清单同步、`docs/tables/` 新增三份契约。⑦ `check-source` 环节的 ODS 张数由硬编码 7 改为从主题声明派生 —— 新增一张表不必再改常量。
- 验证（三处反向实测全部实做，这是本批最重要的证据）：静态 —— `make lint` 全绿。端到端 —— 两期样本整条日批 **18/18 全绿**，耗时 **3 分 58 秒**；`dbt test` **33/33**（新增 10 条）；`verify-ads` 26 项含新增「对账基准独立性」两期全过（E 项各 6 行、司库口径基准 6 行、调节项非零 6 行、最大残差 0.00）。PG 侧回读：Section E 12 行全部 `benchmark_source = TREASURY_CASH_POSITION`、`variance = 0.00`、`status = PASS`，调节项 2,207.64 ~ 222,757.66。**探针 A（改坏基准侧）**：把 ENT002 一个代理行账户的对账单余额加 100,000（占该 Section 0.3%，**低于旧容差 1%**）→ 该实体行与合并行双双 `FAIL`、`variance = 100,000.00`；改回即复绿。**探针 B（退回自比对，模型级）**：把服务端模型副本的 Section E 基准改回总账 → 对账 12 行**全部 PASS**（`benchmark_source = GL`、`benchmark_amount` 与 `fr2052a_amount` 完全相等、残差恰好等于调节项 29,753.86 却落在 1% 容差内）—— 缺陷在对账表上完全隐形，只有 `assert_recon_benchmark_independent` 报 FAIL 12 行。**探针 C（数字凑同值，数据级）**：把 ENT002 的对账单余额搬成账面数、调节项置零 → 该行 `PASS` 而断言 FAIL 1 行、`reason = 基准与报送同值`。复位后重跑整条日批 18/18 复绿，E 项回到司库口径与原始金额。
- 备注（为什么不能只把基准换成「另一张 ODS 快照」）：审查建议原文写的是「换一个可见的独立来源，如 ODS 现金科目快照」，但那仍是同一份总账数据的上游，换表不换源、独立度为零。真独立要换**来源系统 + 口径 + 时点** —— 这里取的是司库系统的对账单/盘点口径，对应的就是真实世界的银行对账（账面 vs 对账单，差额由未达账项解释）。
- 备注（探针 A 顺带证出容差是「对账查不出错」的另一半原因）：100,000 的错误占该 Section 0.3%，旧口径下会被 1% 容差放行；调节项既然已经逐项列出，残差就该是零，因此对账单口径基准的容差收到 1 分钱。
- 备注（演示环境的独立度边界，已登记 `#recon-benchmark-synthetic`）：对账单余额在演示里由生成器从账面倒推，独立度不靠「两个真实系统」，而靠两个不同口径 + 差额必须被调节项逐项解释 + 三条断言守住前提。
- 备注（同步遗留，本批顺带处理）：`oneoff/06_rebuild_owd_history.sql` 补入新表的 DROP —— 复位脚本用它重建干净基线，不补会让重置后残留这张表的旧版本行。
- 回滚：`git revert` 本次提交；数据湖侧需删除 `bronze.ods_treasury_cash_position` 与其 silver 快照/历史表并删 Kafka 主题 `treasury_cash_position`；PG 侧需把 `benchmark_amount` 改回 `gl_amount` 并删两列（`benchmark_source`、`reconciling_item_usd`）。回退后 Section E 恢复「两侧同读总账」，对账重新退化为自证。

## c7-audit-remaining-four

- 范围：`python/exporters/export_gold_to_pg.py`、`deploy/server2/run-daily-pipeline.sh`、`deploy/server1/airflow/dags/fr2052a_backfill_and_restate.py`、`python/lakehouse/{owd_scd2,verify_gold}.py`、`python/generators/{ods_data,ref_data}.py`、`dbt/models/marts/{ads_fr2052a_report.sql,schema.yml}`、`sql/postgres/10_control_tables.sql`、`config/liquidity_thresholds.json`、`docs/business/{DATA-DESIGN,INTERFACE-DESIGN,MODULE-DESIGN,KNOWN-ISSUE}.md`、`docs/tables/{ads_fr2052a_report,ads_liquidity_metrics}.md`、`docs/rules/ACCEPTANCE-CHECKLIST.md`、`docs/AUDIT-2026-09-17-delegate-review.md`
- 变更（审计剩余 4 条一次收口）：
  ① **#3 覆盖写前置闸**：写前业务前提从四条扩到五条，新增「已报送报告期的内容未变」。判据是**内容指纹**（库内现值 vs 本次要写的值，按报告期、按共有列、行序无关）而不是「有没有报送过」—— 日批本身可重跑，拿「已报送」直接拦会挡掉正常重跑。带显式开关 `--allow-after-submission`（等价环境变量 `ALLOW_EXPORT_AFTER_SUBMISSION=1`），由重述流程的 rebuild 步骤携带；开关必须转成命令行参数传进容器（spark-submit 包装脚本只透传白名单变量，环境变量进不去 —— 这一条是本批实测踩出来的）。DAG 顺序（publish-access 先于 export-pg）在 C5/C6 已修正，本批核对一致。
  ② **#16 HQLA 到期窗口**：HQLA 存量排除剩余期限 30 天以内的证券（它们已按 100% 计入 30 天流入），排除部分单列 `sec_i_unencumbered_near_maturity` 以保持「未受限五项 + 已受限 = Section G 合计」；新增 `verify_gold.check_section_i_identity`（恒等式 + 六个桶逐项独立复算）。**顺带修掉「规则在但场景不在」**：样本里证券最短剩余期限 71 天，这条规则原本无对象可判 —— 生成器补入 30 天以内到期的短期票据（`SHORT_DATED_SECURITY_RATIO = 0.15`，类型限国债与机构债），规则因此可验。
  ③ **#14 累计缺口列**：删除 `sec_k_cumulative_30d_gap`（PG 列、模型、schema.yml、表契约、DATA-DESIGN 同步）。先按审计的第一个选项真做了一版（逐桶累计取最低点 + 独立复算 + 「两列不得恒等」判据），实测该判据当场失败：两期 6/6 行两列相等 —— LCR 形状的数据里逐桶累计净现金流单调递减，最低点就是窗口末累计值，也就是净缺口。结论是这个框架下「30 天累计缺口」不是独立指标。
  ④ **#20 SCD2**：`etl_source_file` 进 `EXCLUDED_FROM_HASH`；写后自检增设「每键至多一个有效版本」不变式；改哈希定义属基线重置类变更（同批按纪律跑 `oneoff/06` 重建基线），纪律写进代码注释与 KNOWN-ISSUE。
  ⑤ **VDQ-018 判据重写**（数据变更牵出的既有缺陷）：原判据 `sec_k_total_inflows <= 0.75 * sec_k_total_outflows` 在上限真正生效时必然误报 —— 报出的金额是两位小数，精确值上取整到分会高出至多半厘，实测 6/6 行超限 0.0025~0.005 元。新判据 `abs(sec_k_total_inflows - least(sec_h_expected_inflow_30d, 0.75 * sec_k_total_outflows)) <= 0.01`：判「认列额算得对不对」，留 1 分表示精度容差。
- 验证：静态 —— `make lint` 全绿。端到端 —— 两期样本整条日批 **18/18 全绿**（含新数据），`dbt test` 33/33，`verify-ads` **30 项**全过（新增 Section I/G 恒等式两期、非受限各桶独立复算两期，最大偏差均 0.00），`dq-rules` PASS 11 / FAIL 3（VDQ-017 二级资产占比 WARNING、VDQ-020 LEI、VDQ-021 质押标记缺报，均非 ERROR、不阻断），`owd-scd2` 8 张表 0 变更。**四处反向实测**：㈠ 已报送期内容变化（改口径后重跑）→ 导出被拦、一张表未写、整批退 1；带开关 → 放行并写出（gold 12 / 1220 / 96 行回读一致）；内容一致的重跑 → `[OK] 已报送期 ['2026-09-16'] 的内容与库内一致（重跑幂等）` 且 18/18 全绿。㈡ 撤掉 HQLA 到期窗口（服务端副本）→ `Section I/G 恒等式` 与 `非受限各桶独立复算` 双双 FAIL，偏差恰为被排除的 1,557,684,923.74；从仓库重新同步后复绿。㈢ 往版本历史插一行重复有效版本 → `owd_scd2` 报 `有 1 个键存在多个有效版本（is_active 为真）` 并把该表列为失败；按纪律重建基线后归并为 0 变更。㈣ VDQ-018 反向：新判据在「上限已生效」的数据上 PASS（旧判据在同一数据上 6 行违规）。
- 备注（基线刷新）：证券到期分布与流入上限都会变 —— LCR 基线区间 4.07~11.96 → **7.76~31.12**（`config/liquidity_thresholds.json` 的说明同步）；流入上限从「未触发」变成「已生效」（合并口径未限制前 1,597,613,961.29 vs 上限 589,041,510.44），这正是短期票据入样本的直接效果。
- 备注（同一类缺陷的两种形态，值得记住）：本批两次撞上「规则在、场景不在」—— HQLA 到期窗口在样本上无对象（最短 71 天），VDQ-018 在样本上从未满足触发条件（上限从未生效）。两处都是「代码看着对、跑着绿」，只有把数据改到规则的作用域里才看得出来。教训：**新规则落地时要同时问「样本里有触发它的数据吗」**，没有就补数据，否则规则等于没验过。
- 回滚：`git revert` 本次提交；数据侧需回滚生成器（短期票据比例）并重生成样本、重建 OWD 版本基线；PG 侧需删 `sec_i_unencumbered_near_maturity`、恢复 `sec_k_cumulative_30d_gap`。回退后 HQLA 重新把 30 天内到期的证券计进存量（双向计量回归）。
